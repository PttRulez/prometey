import { Typography } from '@mui/material';
import { FC } from 'react';

const SingleNetwork: FC = () => {
  return (
    <>
      <Typography variant="h3">Страница одной сети</Typography>
    </>
  );
};

export default SingleNetwork;
